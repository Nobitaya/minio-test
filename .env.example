# Credentials are configured in the browser for each temporary test session.
# This file is optional and only controls service-level values.
# 50 MiB per upload. Increase only when the test-server memory limit supports it.
MAX_UPLOAD_BYTES=52428800
# WEB_PORT is the Docker host port; PORT is the Node process port.
WEB_PORT=8085
PORT=8085
